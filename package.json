{
  "name": "mip-paper",
  "version": "0.3.4",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test",
    "check": "find src test scripts -type f -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "release:aur": "node scripts/prepare-aur-release.mjs"
  },
  "dependencies": {
    "@particle/dbus-next": "0.11.4",
    "fft.js": "4.0.4",
    "qweather-icons": "1.8.0"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
