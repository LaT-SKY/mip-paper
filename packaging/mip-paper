#!/usr/bin/env bash
set -euo pipefail

export MIP_PAPER_MODE=packaged
export MIP_PAPER_SOURCE_ROOT=/usr/lib/mip-paper
export MIP_PAPER_INSTALL_ROOT=/usr/lib/mip-paper
export MIP_PAPER_SERVICE_PATH=/usr/lib/systemd/user/mip-paper.service

exec /usr/lib/mip-paper/bin/mip-paper "$@"
